---
import { cn } from '@lib/utils';

interface Props {
	className: string;
}

const { className } = Astro.props;
---

<article class={cn(className, 'rounded-card p-10 md:p-12')}>
	<slot />
</article>
